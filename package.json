{
  "name": "flat-manager",
  "version": "1.0.0",
  "description": "Complete MERN Flat Manager Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "chai": "^5.3.3",
    "concurrently": "^7.0.0",
    "mocha": "^11.7.2",
    "mochawesome": "^7.1.4",
    "nyc": "^17.1.0",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4"
  },
  "author": "Flat Manager Team",
  "license": "ISC"
}
